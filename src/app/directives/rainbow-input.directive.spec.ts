import { RainbowInputDirective } from './rainbow-input.directive';

describe('RainbowInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowInputDirective();
    expect(directive).toBeTruthy();
  });
});
